$kind: http-request
name: Login
method: POST
url: "{{baseUrl}}/api/auth/login"
order: 1000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "admin@studia.local",
      "password": "Admin123!"
    }
